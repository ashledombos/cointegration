# Pairs Trading Bot - Configuration
# Copier ce fichier en .env et remplir les valeurs

# === TELEGRAM ===
# Créer un bot via @BotFather sur Telegram
# Le chat_id peut être obtenu via https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === DISCORD ===
# Créer un webhook dans Paramètres serveur > Intégrations > Webhooks
DISCORD_WEBHOOK_URL=

# === NTFY (recommandé pour mobile /e/OS) ===
# 1. Installer l'app Ntfy depuis F-Droid ou https://ntfy.sh
# 2. S'abonner à un topic unique (ex: cointegration_raph_xyz123)
# 3. Utiliser ce topic ci-dessous
NTFY_TOPIC=
NTFY_SERVER=https://ntfy.sh

# === FTMO ACCOUNT ===
# Taille du compte FTMO (en USD ou EUR selon ton compte)
FTMO_ACCOUNT_SIZE=100000
# Risque max par trade (0.015 = 1.5%)
FTMO_RISK_PER_TRADE=0.015
# Drawdown daily max FTMO = 5%, on met une marge de sécurité
FTMO_MAX_DAILY_RISK=0.04

# === OPTIONNEL ===
# Pour les exchanges crypto (si utilisation de CCXT)
# BINANCE_API_KEY=
# BINANCE_API_SECRET=

